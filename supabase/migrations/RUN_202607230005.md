# Apply migration 202607230005_domain_shell_services.sql

## 前提

先に以下を適用してください。

1. `202607230003_dictionaries.sql`
2. `202607230004_domain_service_details.sql`

## 適用方法

Supabase SQL Editor で `202607230005_domain_shell_services.sql` を実行してください。

または（推奨・冪等）:

```bash
npx tsx scripts/seed-domain-shell-services.ts
```

`domain_service_details` 未作成の場合はサービス本体のみ登録し、詳細行はスキップします。
004 適用後に同じスクリプトを再実行すると詳細行を補完します。

## 追加内容

- ドメイン図鑑の初期サービス8件（下書き・本サイト非表示・affiliate_url=null・ASP未設定）
- 既存 `(dictionary_id, slug)` は重複挿入せず、name / official_url / display_order の不足分のみ補完
- 欠落している `domain_service_details` 行の作成
