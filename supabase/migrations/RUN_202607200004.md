# Run: 202607200004_fix_plan_representative_columns.sql

**Required to fix「公開に失敗しました」** when draft plans include
`is_default_comparison_plan` / `is_recommended` / `official_url`.

Apply in Supabase SQL Editor now if publish fails with:

`column service_plans.is_default_comparison_plan does not exist`
