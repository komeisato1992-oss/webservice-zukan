# Run: 202607220001_plan_count_fields_to_text.sql

プランの「データベース数」「無料ドメイン数」「マルチドメイン数」を
integer → text に変更します（「無制限」等を保存するため）。

Apply in Supabase SQL Editor:

`supabase/migrations/202607220001_plan_count_fields_to_text.sql`

既存の数値は文字列へキャストして保持されます。
