# Apply migration 202607230007_ranking_dictionary_id.sql

前提: `202607230003_dictionaries.sql` 適用済みであること。

## 1. SQL Editor

Supabase SQL Editor で `202607230007_ranking_dictionary_id.sql` を実行してください。

追加内容:
- `ranking_entries.dictionary_id`（NOT NULL + unique `(dictionary_id, purpose_id, rank)`）
- `cms_ranking_drafts.dictionary_id`（図鑑ごと1行・シングルトン廃止）
- `ranking_publish_history.dictionary_id`
- 既存行の dictionary_id バックフィル（services 経由）
- domain 用 draft 行の作成

## 2. データ復旧スクリプト

```bash
npx tsx scripts/repair-ranking-dictionary-separation.ts
```

- サーバー図鑑の「総合おすすめ」を `ranking_publish_history.old_value` から復旧
- 図鑑別 draft を公開エントリから再構築
