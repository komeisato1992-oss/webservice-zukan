# Run migration: 202607200003_service_affiliate_fields

Adds affiliate management columns on `services`:

- `affiliate_url` (TEXT)
- `affiliate_network` (TEXT, default `A8`)
- `affiliate_status` (TEXT, default `inactive`)

## Apply

In Supabase SQL Editor, run:

`supabase/migrations/202607200003_service_affiliate_fields.sql`

Or via CLI:

```bash
supabase db push
```

## Notes

- These fields are hand-managed. Scraping must never overwrite them.
- Public 「公式サイトへ」 uses `affiliate_url || official_url`.
