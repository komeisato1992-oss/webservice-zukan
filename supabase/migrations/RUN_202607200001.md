# Run migration 202607200001_cms_draft_publish

## What it adds

- Draft/publish tables: `cms_service_drafts`, `cms_change_items`, `cms_publish_events`, `cms_publish_history_items`
- `scraping_candidates`, `service_campaigns`
- Service/plan CMS columns + comparison field draft layout flags
- Seed comparison fields for TOP / compare display

## How to apply

In Supabase SQL editor (or CLI), run:

`supabase/migrations/202607200001_cms_draft_publish.sql`

Safe to re-run: triggers use `drop trigger if exists`, tables use `create table if not exists`, seeds use `on conflict do update`.

## After apply

1. Restart `npm run dev`
2. Open `/admin/services` — CMS sidebar should show the new IA
3. Edit a service → 下書き保存 → 公開へ反映
